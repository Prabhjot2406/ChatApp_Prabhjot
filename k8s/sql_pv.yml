apiVersion: v1
kind: PersistentVolume
metadata:
  name: sql-pv-volume
  namespace: chat-app
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data"
